export type TypePsychologicalAssistant = {
  id: string;
  nameAuthor: string;
  description: string;
  authorPhoto: {
    alt: string;
    url: string;
  };
};

export type TypeBanner = {
  id: string;
  title: string;
  photo: {
    alt: string;
    url: string;
  };
};
