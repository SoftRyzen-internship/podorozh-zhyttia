export type TypePsychologist = {
  id: string;
  name: string;
  description: string;
  psychologistPhoto: {
    alt: string;
    url: string;
  };
};

export type TypeBanner = {
  id: string;
  title: string;
  photo: {
    alt: string;
    url: string;
  };
};
